{
  "name": "shareit-firestore-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "cp ../firestore.rules ./firestore.rules",
    "test": "firebase emulators:exec --only firestore --project demo-shareit \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
