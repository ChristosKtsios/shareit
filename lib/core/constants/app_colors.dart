import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds (warm dark)
  static const background     = Color(0xFF0F0A14);
  static const surface        = Color(0xFF1F1418);
  static const surfaceVariant = Color(0xFF2A1D22);

  // Brand: Coral (Airbnb-inspired)
  static const primary        = Color(0xFFFF6B47);
  static const primarySurface = Color(0xFF3D1A12);

  // Functional
  static const offer          = Color(0xFF10B981);
  static const seek           = Color(0xFF3B82F6);
  static const deal           = Color(0xFFFBBF24);
  static const danger         = Color(0xFFEF4444);
  static const success        = Color(0xFF34D399);

  // Text
  static const textPrimary    = Color(0xFFFFF7ED);
  static const textSecondary  = Color(0xFFA8A29E);
  static const textHint       = Color(0xFF6B5B5F);

  // Borders
  static const border         = Color(0xFF2D1F25);
  static const borderLight    = Color(0xFF3A2A30);
}
